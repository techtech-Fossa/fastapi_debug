version: "3.9"
services:
  fastapi:
    build:
      context: ./app
      dockerfile: Dockerfile
    volumes:
      - ./app:/app
    ports:
      - "8000:8000" # FastAPI
      - "5678:5678" # debugpy
